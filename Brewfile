# empty file

is_stripe = Dir.exist?("/Applications/Santa.app")

brew "git"

# Ruby
brew "openssl"
brew "rbenv"
brew "ruby-build"

if OS.mac?
  if !is_stripe
    brew "restic"
  end
end

# vim: set ft=ruby
